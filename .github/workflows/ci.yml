on:
  pull_request:
  push:
    branches: ["**"]

jobs:
  validate:
    runs-on: ubuntu-latest

    steps:
      - uses: denoland/setup-deno@v2
        with:
          deno-version: lts

      - name: Validate JSON
        run: deno ./scripts/validate.ts
